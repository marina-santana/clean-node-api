{
  "name": "clean-node-api",
  "version": "1.0.0",
  "description": "Repositório do curso de NodeJs, TDD e Clean Architecture.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "lint-staged": {
    "*.js": [
      "standard --fix",
      "git add"
    ]
  },
  "husky":{
    "hooks":{
      "pre-commit": "lint-staged"
    }
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^0.15.0-rc.12",
    "lint-staged": "^10.0.8",
    "standard": "^14.3.3"
  }
}
